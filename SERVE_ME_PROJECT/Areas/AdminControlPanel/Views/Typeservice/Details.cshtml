@model SERVE_ME_PROJECT.Models.TypeserviceModel

@{
    Layout = "_LayoutAdmin";
    ViewData["Title"] = "Details";
}

<h1>تفاصيل نوع الخدمة </h1>

<div>
     <hr />
    <dl class="row">
        <dt class = "col-sm-2">الاسم         </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Reply" asp-route-id="@Model.Id" class="btn btn-sm btn-primary">تعديل </a>
    <a asp-action="Index" class="btn btn-sm btn-primary"> رجوع</a>
</div>
